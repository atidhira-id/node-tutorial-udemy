extends layouts/main-layout.pug

block styles 
    link(rel="stylesheet", href="/css/product.css")

block content
    main 
        if users.length > 0
            .grid
                each user in users 
                    article.card.product-item
                        header.card__header
                            h1.product__title #{user.name}
        else
            h1 No user